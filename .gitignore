# OS
.DS_Store
Thumbs.db

# logs
*.log

# dependencies (none expected — vanilla project)
node_modules/

# secrets — never commit API keys (the OpenAI key lives only in localStorage)
.env
.env.*
*.key
secrets.*

# editors
.vscode/
.idea/
*.swp

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example
